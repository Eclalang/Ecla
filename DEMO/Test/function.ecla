import console;

function power(base : int, a : int)(int) {
    if (a != 0){
        return (base * power(base, a - 1));
    }else{
        return 1;
    }
}

console.println(power(12,18));
console.println(power(12,18));
console.println(power(12,18));